# tup = 'Hello'
# print(len(tup))

tup = 'Hello',
print(type(tup))
print(len(tup))

# in line 1 first one tup is just a variable not an actual tuple because the correct syntax hasn't been used
# it prints the length of the word hello and doesn't treat it as a tuple
# in line 4 the comma has been used so the tup variable is a tuple
# line 5 prints the number of tuples